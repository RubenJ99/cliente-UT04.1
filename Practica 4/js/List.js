'use strict';

function List(data){
    if(!this instanceof List) throw new Error("CUSTOM ERROR"); 
    if(typeof(data) === "object") throw new Error("CUSTOM ERROR");
    
    let _storage = [];
    

}
